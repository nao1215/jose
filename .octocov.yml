# generated by octocov init
coverage:
  if: true
  acceptable: 90%
  badge:
    path: docs/coverage.svg
testExecutionTime:
  if: true
  badge:
    path: docs/time.svg
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
codeToTestRatio:
  badge:
    path: docs/ratio.svg
